# Welcome to VIZO | Visual Insights & Zero Obstacles

Run the backend
```bash
cd backend
uv run uvicorn main:app --reload --port 8000
```



Run the frontend

```bash
cd frontend
bun install
bun run dev
```